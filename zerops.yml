zerops:
  setup: nginx0

  build:
    base: nodejs@20
    buildCommands:
      - npm i
      - npm run build
    deployFiles:
      - ./dist/~

  cache:
    - node_modules
    - package-lock.json

  run:
    base: nginx@1.22
    siteConfigPath: site.conf

